    <div class="side-header">
      <div class="header-name">
        <%= link_to current_user.name, edit_user_path(current_user)%>
      </div>
      <div class="chat-new">
        <%= link_to 'チャットを作成する', new_room_path%>
      </div>
    </div>

    <div class="bar">
      <div class="side-room">
        <div class="room-sentens">
<% current_user.rooms.each do |room| %>
    <div class="room">
      <div class="room-name">
        <a href="#"><%= room.name %></a>
      </div>
    </div>
  <% end %>
        </div>
      </div>
    </div>